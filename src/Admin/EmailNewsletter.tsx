import React, { useState } from "react";
const elementosIniciales = ["Elemento 1", "Elemento 2", "Elemento 3"];

function EmailNewsletter() {
  /*  const usuariosIniciales: Usuario[] = [

  copyemail


    { nombre: "Juan", email: "juan@example.com" },
    { nombre: "María", email: "maria@example.com" },
    { nombre: "Pedro", email: "pedro@example.com" }
  ];
  const [usuarios, setUsuarios] = useState<Usuario[]>(usuariosIniciales);

  function copyToClipboard() {
    const emails = usuarios.map((usuario) => usuario.email).join("\n");
    navigator.clipboard.writeText(emails);
  }

  function agregarUsuario(usuario: Usuario) {
    setUsuarios([...usuarios, usuario]);
  }*/
  /*return (
    <div>
      <button onClick={() => agregarElemento("Elemento 1")}>
        Agregar elemento
      </button>
      <button onClick={copyToClipboard}>Copiar al portapapeles</button>
      <ul>
        {estado.map((elemento, indice) => (
          <li key={indice}>{elemento}</li>
        ))}
      </ul>
    </div>
  );*/
}
export default EmailNewsletter;
